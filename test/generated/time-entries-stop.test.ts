/**
 * This file was generated by running `npm run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst time-entries stop", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js time-entries stop --help"
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js time-entries stop

    Stopping a time entry is only possible if it’s currently running

    Options:
          --help           Show help                                       [boolean]
      -t, --time_entry_id  The ID of the time entry you're stopping.      [required]
      -f, --fields         Comma separated list of fields to display in the output.
      -o, --output         The output format: json, table"
  `);
});
