/**
 * This file was generated by running `bun run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst invoices messages list-by-invoice", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js invoices messages list-by-invoice --help",
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js invoices messages list-by-invoice

    Returns a list of messages associated with a given invoice

    Options:
          --help           Show help                                       [boolean]
      -i, --invoice_id     The ID of the invoice that contains the messages you are
                           listing.                                       [required]
      -u, --updated_since  Only return invoice messages that have been updated since
                            the given date and time.
      -p, --page           The page number to use in pagination. Use \`all\` to retrie
                           ve all pages.
      -e, --per_page       The number of records to return per page. Can range betwe
                           en 1 and 2000.
      -f, --fields         Comma separated list of fields to display in the output.
      -o, --output         The output format: json, table"
  `);
});
