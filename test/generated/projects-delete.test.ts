/**
 * This file was generated by running `bun run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst projects delete", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js projects delete --help",
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js projects delete

    Deletes a project and any time entries or expenses tracked to it

    Options:
          --help        Show help                                          [boolean]
      -p, --project_id  The ID of the project you're deleting.            [required]
      -f, --fields      Comma separated list of fields to display in the output.
      -o, --output      The output format: json, table"
  `);
});
