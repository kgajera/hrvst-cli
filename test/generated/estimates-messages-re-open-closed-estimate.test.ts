/**
 * This file was generated by running `bun run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst estimates messages re-open-closed-estimate", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js estimates messages re-open-closed-estimate --help",
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js estimates messages re-open-closed-estimate

    Creates a new estimate message object and re-opens a closed estimate

    Options:
          --help         Show help                                         [boolean]
      -e, --estimate_id  The ID of the estimate you're re-opening.        [required]
      -v, --event_type   Pass "re-open" to re-open the estimate           [required]
      -f, --fields       Comma separated list of fields to display in the output.
      -o, --output       The output format: json, table"
  `);
});
