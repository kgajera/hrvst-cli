/**
 * This file was generated by running `npm run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst estimates line-items delete", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js estimates line-items delete --help"
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js estimates line-items delete

    Update an exisitng line item on an estimate

    Options:
          --help                   Show help                               [boolean]
      -e, --estimate_id            The ID of the estimate that contains the line ite
                                   m you're deleting.                     [required]
      -l, --line_items[0]id        The ID of the line item you want to delete.
                                                                          [required]
      -i, --line_items[0]_destroy  Gives the command to delete the line item.
                                                                          [required]
      -f, --fields                 Comma separated list of fields to display in the
                                   output.
      -o, --output                 The output format: json, table"
  `);
});
