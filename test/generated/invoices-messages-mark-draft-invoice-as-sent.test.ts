/**
 * This file was generated by running `bun run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst invoices messages mark-draft-invoice-as-sent", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js invoices messages mark-draft-invoice-as-sent --help",
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js invoices messages mark-draft-invoice-as-sent

    Creates a new invoice message object and marks the invoice as sent

    Options:
          --help        Show help                                          [boolean]
      -i, --invoice_id  The ID of the invoice you're sending.             [required]
      -e, --event_type  Pass “send” to mark the invoice as sent.          [required]
      -f, --fields      Comma separated list of fields to display in the output.
      -o, --output      The output format: json, table"
  `);
});
