/**
 * This file was generated by running `npm run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst invoices messages mark-open-invoice-as-draft", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js invoices messages mark-open-invoice-as-draft --help"
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js invoices messages mark-open-invoice-as-draft

    Creates a new invoice message object and marks an open invoice as a draft

    Options:
          --help        Show help                                          [boolean]
      -i, --invoice_id  The ID of the invoice you're marking as a draft.  [required]
      -e, --event_type  Pass “draft” to mark the invoice as a draft.      [required]
      -f, --fields      Comma separated list of fields to display in the output.
      -o, --output      The output format: json, table"
  `);
});
