/**
 * This file was generated by running `bun run generate` and should NOT be modified.
 */
import { execaCommand } from "execa";
import { expect, test } from "vitest";

test("hrvst estimates line-items update", async () => {
  const { stdout } = await execaCommand(
    "node dist/cli.js estimates line-items update --help",
  );
  expect(stdout).toMatchInlineSnapshot(`
    "cli.js estimates line-items update

    Update an exisitng line item on an estimate

    Options:
          --help                      Show help                            [boolean]
      -e, --estimate_id               The ID of the estimate that contains the line
                                      item you're updating.               [required]
      -l, --line_items[0]id           The ID of the line item you want to update.
                                                                          [required]
      -i, --line_items[0]kind         The name of an estimate item category.
      -n, --line_items[0]description  Text description of the line item.
      -t, --line_items[0]unit_price   The individual price per unit.
      -m, --line_items[0]quantity     The unit quantity of the item.
      -s, --line_items[0]taxed        Whether the estimate's tax percentage applies
                                       to this line item.
      -a, --line_items[0]taxed2       Whether the estimate's tax2 percentage applie
                                      s to this line item.

      -f, --fields                    Comma separated list of fields to display in t
                                      he output.
      -o, --output                    The output format: json, table"
  `);
});
